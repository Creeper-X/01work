# NLPrompt 项目介绍文档

## 项目概述

**NLPrompt** (Noise-Label Prompt Learning for Vision-Language Models) 是一个用于视觉-语言模型的噪声标签提示学习框架，是 **CVPR 2025 Highlight** 论文的官方 PyTorch 实现。

### 核心功能
该项目主要解决在存在标签噪声的情况下，如何有效地训练视觉-语言模型（如 CLIP）进行图像分类任务。它通过提示学习（Prompt Learning）技术，使模型能够在含有噪声标签的数据集上进行鲁棒的学习。

### 技术特点
- 基于 CLIP（Contrastive Language-Image Pre-training）模型
- 支持对称噪声（Symmetric）和非对称噪声（Asymmetric）两种噪声类型
- 使用最优传输（Optimal Transport）技术处理噪声标签
- 支持 Few-shot Learning（少样本学习）
- 可配置的上下文向量学习

### 支持的数据集
项目支持多个主流图像分类数据集：
- **标准数据集**: Caltech101, Food101, Food101N, OxfordPets, OxfordFlowers, FGVCAircraft, DTD, EuroSAT, StanfordCars, SUN397, UCF101
- **ImageNet 系列**: ImageNet, ImageNetV2, ImageNet-A, ImageNet-R, ImageNet-Sketch

---

## 环境安装

### 前置要求
- Anaconda/Miniconda
- CUDA 支持的 GPU（推荐）

### 安装步骤

```bash
# 1. 克隆项目仓库
git clone https://github.com/qunovo/NLPrompt.git
cd NLPrompt/Dassl.pytorch

# 2. 创建 conda 虚拟环境
conda create -y -n nlprompt python=3.8

# 3. 激活环境
conda activate nlprompt

# 4. 安装 PyTorch（根据你的 CUDA 版本调整）
conda install pytorch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 pytorch-cuda=12.1 -c pytorch -c nvidia

# 5. 安装项目依赖
pip install -r requirements.txt

# 6. 安装 Dassl 库（开发模式）
python setup.py develop

# 7. 返回项目根目录
cd ..
```

### 数据集准备
1. 按照 [DATASETS.md](https://github.com/KaiyangZhou/CoOp/blob/main/DATASETS.md) 的说明准备各个数据集
2. 将数据集放在 `./data` 目录下
3. **特别注意**: Food101N 数据集需要从 [Kaggle](https://www.kaggle.com/datasets/kuanghueilee/food-101n) 单独下载

---

## 项目结构

```
NLPrompt/
├── train.py                    # 主训练脚本
├── test.py                     # 测试脚本
├── utils.py                    # 工具函数
├── clip/                       # CLIP 模型实现
│   ├── clip.py
│   ├── model.py
│   └── simple_tokenizer.py
├── configs/                    # 配置文件目录
│   ├── datasets/              # 数据集配置
│   └── trainers/              # 训练器配置
│       └── NLPrompt/          # NLPrompt 方法配置
├── datasets/                   # 数据集加载器
├── trainers/                   # 训练器实现
│   └── nlprompt.py            # NLPrompt 核心实现
├── scripts/                    # 运行脚本
│   └── nlprompt/
│       ├── main.sh            # 主训练脚本
│       └── eval.sh            # 评估脚本
├── data/                       # 数据集存放目录
├── output/                     # 实验结果输出目录
└── Dassl.pytorch/             # Dassl 库（深度学习工具库）
```

---

## 如何运行

### 训练模型

项目提供了便捷的 shell 脚本来运行实验。运行前请确保数据路径正确。

#### 基本命令格式
```bash
bash scripts/nlprompt/main.sh <数据集> <shots数量> <噪声率> <噪声类型> <类别数量>
```

#### 参数说明
- **数据集**: 数据集名称（如 `caltech101`, `food101` 等）
- **shots数量**: Few-shot 设置中每类的样本数（如 16）
- **噪声率**: 标签噪声比例（如 0.50 表示 50% 的标签被污染）
- **噪声类型**: 
  - `sym`: 对称噪声（标签随机替换为任意类别）
  - `asym`: 非对称噪声（标签替换遵循特定模式）
- **类别数量**: 数据集的类别总数

#### 示例 1: Caltech101 对称噪声实验
```bash
bash scripts/nlprompt/main.sh caltech101 16 0.50 'sym' 100
```
- 在 Caltech101 数据集上训练
- 每类使用 16 个样本
- 50% 的标签噪声率
- 对称噪声类型
- 100 个类别

#### 示例 2: Caltech101 非对称噪声实验
```bash
bash scripts/nlprompt/main.sh caltech101 16 0.50 'asym' 100
```
- 在 Caltech101 数据集上训练
- 每类使用 16 个样本
- 50% 的标签噪声率
- 非对称噪声类型
- 100 个类别

#### 示例 3: Food101 实验
```bash
bash scripts/nlprompt/main.sh food101 16 0.40 'sym' 101
```

### 直接使用 Python 训练
```bash
python train.py \
    --root ./data \
    --seed 1 \
    --trainer NLPrompt \
    --dataset-config-file configs/datasets/caltech101.yaml \
    --config-file configs/trainers/NLPrompt/rn50.yaml \
    --output-dir output/caltech101/NLPrompt/test \
    DATASET.NUM_SHOTS 16 \
    DATASET.NOISE_RATE 0.5 \
    DATASET.NOISE_TYPE 'sym' \
    DATASET.num_class 100
```

### 评估模型
```bash
bash scripts/nlprompt/eval.sh <数据集> <配置文件>
```

---

## 配置说明

### 主要配置参数

项目使用 YAML 文件进行配置，主要配置参数包括：

#### 训练器配置 (在 `configs/trainers/NLPrompt/` 下)
- **N_CTX**: 上下文向量数量（默认 16）
- **CSC**: 是否使用类别特定上下文（Class-Specific Context）
- **CTX_INIT**: 上下文初始化词
- **PREC**: 精度设置（fp16, fp32, amp）
- **CLASS_TOKEN_POSITION**: 类别标记位置（'end', 'middle', 'front'）

#### 数据集配置
- **NUM_SHOTS**: Few-shot 设置中的样本数
- **NOISE_RATE**: 噪声率（0.0-1.0）
- **NOISE_TYPE**: 噪声类型（'sym' 或 'asym'）
- **USE_OT**: 是否使用最优传输
- **REG_FEAT**: 特征正则化参数
- **REG_LAB**: 标签正则化参数
- **CURRICLUM_EPOCH**: 课程学习的 epoch 数

### 可用的模型配置
- `rn50.yaml`: ResNet-50 backbone
- `rn101.yaml`: ResNet-101 backbone
- `vit_b16.yaml`: Vision Transformer B/16
- `vit_b32.yaml`: Vision Transformer B/32
- `*_ep50.yaml`: 训练 50 个 epoch 的版本
- `*_ep100.yaml`: 训练 100 个 epoch 的版本

---

## 输出结果

训练完成后，结果会保存在 `output/` 目录下，结构如下：
```
output/
└── <数据集名>/
    └── NLPrompt/
        └── <配置>_<shots>shots/
            └── noise_<类型>_<噪声率>/
                └── seed<种子>/
                    ├── log.txt           # 训练日志
                    ├── model-best.pth.tar # 最佳模型
                    └── ...
```

---

## 核心技术原理

### 1. 提示学习（Prompt Learning）
通过学习可训练的上下文向量（context vectors），将类别名称转换为更有效的提示文本，使 CLIP 模型能够更好地适应特定任务。

### 2. 噪声标签处理
- **对称噪声**: 标签随机被替换为任意其他类别
- **非对称噪声**: 标签的错误遵循特定的混淆模式（更接近真实场景）

### 3. 最优传输（Optimal Transport）
使用最优传输理论来对齐特征空间和标签空间，有效减轻噪声标签的影响。

### 4. 课程学习（Curriculum Learning）
逐步增加训练样本的难度，先学习容易的（可能是干净的）样本，再学习困难的（可能含噪声的）样本。

---

## 依赖项目

本项目基于以下优秀的开源项目构建：
- **CoOp**: Context Optimization for Prompt Learning
- **Dassl**: A PyTorch toolbox for domain adaptation and semi-supervised learning
- **CLIP**: OpenAI 的 Contrastive Language-Image Pre-training 模型

---

## 论文引用

如果这个项目对你的研究有帮助，请引用：

```bibtex
@inproceedings{pan2025nlprompt,
  title={NLPrompt: Noise-Label Prompt Learning for Vision-Language Models},
  author={Pan, Bikang and Li, Qun and Tang, Xiaoying and Huang, Wei and Fang, Zhen and Liu, Feng and Wang, Jingya and Yu, Jingyi and Shi, Ye},
  booktitle={Proceedings of the Computer Vision and Pattern Recognition Conference},
  pages={19963--19973},
  year={2025}
}
```

---

## 常见问题

### 1. 数据路径错误
确保在 `scripts/nlprompt/main.sh` 中的 `DATA` 变量指向正确的数据目录。

### 2. CUDA 内存不足
尝试：
- 减小 batch size
- 使用更小的模型（如 RN50 而不是 RN101）
- 使用 fp16 精度

### 3. 数据集准备
参考 CoOp 项目的 [DATASETS.md](https://github.com/KaiyangZhou/CoOp/blob/main/DATASETS.md) 获取详细的数据集准备说明。

---

## 项目链接

- **论文**: https://arxiv.org/abs/2412.01256
- **GitHub**: https://github.com/qunovo/NLPrompt

---

**最后更新**: 2025年12月19日
